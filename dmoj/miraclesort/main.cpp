#include <bits/stdc++.h>

using namespace std;

int N;

int main() {
    scanf("%d", &N);
    while (N--) {
        long long num;
        scanf("%lld", &num);
        printf("%lld\n", num);
    }
    return 0;
}